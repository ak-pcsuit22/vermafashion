# Example environment variables for Verma Fashions
# Copy this to a file named .env in development (do NOT commit .env to git)

# Secret key used by Flask for sessions and CSRF
SECRET_KEY=replace-with-a-secure-random-string

# Use a proper DB in production. Example sqlite file path (absolute) or postgres URL
# DATABASE_URL=sqlite:///C:/path/to/app.db
# DATABASE_URL=postgresql://user:pass@localhost/dbname

# Control whether Flask session cookie sets Secure flag (1 = true)
SESSION_COOKIE_SECURE=0
SESSION_COOKIE_SAMESITE=Lax

# Upload limits in bytes (default 5MB)
MAX_CONTENT_LENGTH=5242880

# Allow overriding upload folder
# UPLOAD_FOLDER=C:/path/to/uploads

# SMTP Configuration for sending emails (OTP, password reset, etc.)
# If not configured, emails will be printed to the server console (development mode)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-specific-password
ADMIN_EMAIL=your-email@gmail.com
